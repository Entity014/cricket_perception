# ============================================================
#  Cricket Acoustic Perception — Python Dependencies
# ============================================================
#
#  Install:  pip install -r requirements.txt
#
#  NOTE: cuML (RAPIDS) ต้อง install แยกผ่าน conda หรือ pip
#        ตาม CUDA version ของเครื่อง:
#        https://docs.rapids.ai/install
# ============================================================

# --- Core Audio ---
librosa>=0.10.0
soundfile>=0.12.1
noisereduce>=3.0.0
audioread>=3.0.0

# --- Feature Extraction & Signal Processing ---
numpy>=1.24.0
scipy>=1.11.0

# --- Machine Learning (CPU) ---
scikit-learn>=1.3.0
umap-learn>=0.5.5
hdbscan>=0.8.33

# --- Deep Learning (GPU) ---
torch>=2.1.0
torchaudio>=2.1.0
# torchvision>=0.16.0  # uncomment ถ้าต้องการ spectrogram CNN

# --- PANNs Audio Embeddings (PyTorch-based VGGish/CNN14) ---
# panns-inference>=0.1.0   # uncomment หลัง install torch

# --- Data & Visualization ---
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0

# --- Jupyter ---
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.1.0

# --- Utilities ---
tqdm>=4.65.0
pyyaml>=6.0
python-dotenv>=1.0.0

# ============================================================
#  cuML (RAPIDS) — GPU-accelerated UMAP & HDBSCAN
#  ติดตั้งแยกตาม CUDA version ด้วยคำสั่ง:
#
#  CUDA 12.x:
#    pip install \
#      --extra-index-url=https://pypi.nvidia.com \
#      cuml-cu12==24.10.*
#
#  หรือดูคำสั่งเต็มที่: https://docs.rapids.ai/install#pip
# ============================================================
